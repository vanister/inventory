{
  "name": "inventory-services-express",
  "version": "0.1.0",
  "description": "The Inventory API services as an Express server.",
  "private": true,
  "keywords": [],
  "author": "vanister",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.app.json",
    "build:full": "npm run clean && npm run build",
    "build:watch": "npm run build -- --watch",
    "clean": "rimraf ./build",
    "debug": "npm run build && sls offline",
    "start": "npm run build && sls offline",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "@types/node": "^14.14.13",
    "@types/pg": "^7.14.7",
    "@types/sqlite3": "^3.1.6",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "serverless": "^2.15.0",
    "serverless-offline": "^6.8.0",
    "sqlite3": "^5.0.0",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}